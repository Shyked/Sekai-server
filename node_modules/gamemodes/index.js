// From Ogar

module.exports = {
    Default: require('./Default'),
    Nova: require('./Nova')
};

var get = function(modeId) {
    var mode;
    modeId = String(modeId).toLowerCase();
    switch (modeId) {
        case "nova": // Nova
            mode = new module.exports.Nova();
            break;
        case "default":
        default: // Default
            mode = new module.exports.Default();
            break;
    }
    return mode;
};

module.exports.get = get;

